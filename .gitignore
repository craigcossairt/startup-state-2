# Secrets — never commit real values
.env
.env.*
!.env.example
.mcp.json
*.pem
*.key

# Dependencies / build output (extend per stack)
node_modules/
dist/
build/
.next/
out/
.dart_tool/
__pycache__/
target/

# Local agent state
.claude/settings.local.json

# Weekend federal dumps (too large; pull locally). Committed slices stay in git.
data/cache/sam/active.json
data/cache/sbir/award_data.csv

# OS noise
.DS_Store
Thumbs.db
tsconfig.tsbuildinfo
