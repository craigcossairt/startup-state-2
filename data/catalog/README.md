Committed fallbacks for leftover surfaces.

`resources.json` is the 213-row GOEO directory.
`startups.json` is the 220-row Utah company list, all with lat/lng.

The app prefers live Supabase tables when the public URL and anon key are set.
These files keep the pages full when those keys are missing.
