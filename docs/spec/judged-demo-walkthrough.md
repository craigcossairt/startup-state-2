# Judged demo walkthrough

**Status: LOCKED** — HITL grilling. See [Judged demo walkthrough](https://github.com/craigcossairt/startup-state-2/issues/4).

The default click path if a judge is standing there. Not a required speech. Craig may never narrate it. Build so this path works; do not block the weekend on rehearsal.

Structure: `docs/spec/intake-and-map-look.md`. Floor: `docs/spec/retrieve-and-rank.md`.

## Grilling decisions

| # | Decision |
| --- | --- |
| Q1 | Spoken beats: **Healthcare AI**, then **Youth marketplace (honest-no)**. Fixtures 2–4 are clickable, not narrated unless asked. |
| Q2 | **Fixtures first.** Do not open on a live infer. Optional healthcare sentence only if infer is warm and there is spare time. |
| Q3 | **No bonus clicks.** On-card history covers insight. Checklist, 12-month, chat, alerts, agency map, graph stay off the path even if they shipped. |
| Q4 | If you speak at all, two closer lines after fixture-5: fit never eligible; same map, Federal and State, Utah is the first jurisdiction. Expansion stays in the room, not the footer. |
| Q5 | On fixture-1, point at: Federal badge, Fit `likely`, why, one Utah similar awardee, next step, then one Utah-badge companion card. |

## Click path

1. Land on Intake. Click **Healthcare AI**. Map loads from the fixture (all must-haves `known`). Cached retrieve, not a cold Grants.gov hunt.
2. On the first Ranked card: Federal badge, Fit, why, similar awardee, next step. Then one Utah-badge card (Nucleus / counseling / whatever [Utah State-lane mix](https://github.com/craigcossairt/startup-state-2/issues/13) locks).
3. Fixture rail → **Youth marketplace (honest-no)**. Banner, Utah cards first, 1–3 Federal `probably not` still listed. Do not invent a strong federal grant.
4. Stop. If a judge asks, they can click Aerospace, Water / climate, or Cyber themselves.

## What must be true for this path

- Fixture rail visible on the map.
- Fixture-1 has at least one Federal `likely` or `potential-verify` and at least one Utah companion if a GOEO key fired.
- Fixture-5 trips the probably-not floor. Banner copy is the locked sentence.
- Similar awardees on the card when the cache has them. Empty is allowed; do not invent firms.
- Nav is Startup State + GOEO + Opportunity Map only.

## What you do not do in the room

- Walk all five fixtures as a script.
- Open follow-up chat.
- Click a bonus surface to "show architecture."
- Open on a typed sentence as the first move.
- Put a multi-state expansion note in the footer.

## What this spec is not

- A speech Craig has to give.
- Pixel polish ([Visual end state for Intake and Opportunity Map](https://github.com/craigcossairt/startup-state-2/issues/18)).
- The Utah official-card list ([Utah State-lane mix](https://github.com/craigcossairt/startup-state-2/issues/13)).
- Exact infer / rank / explain prompts.
