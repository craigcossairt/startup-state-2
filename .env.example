# Copy to .env and fill from your password manager. Never commit .env.

# Infer + rank (Grok 4.6 at https://api.x.ai/v1). Password manager: xAI API key.
XAI_API_KEY=

# SAM Assistance Listings join-only cache. Password manager: SAM.gov public API key.
# Do not live-hit SAM on the weekend. Optional after the Active dump exists.
SAM_API_KEY=
